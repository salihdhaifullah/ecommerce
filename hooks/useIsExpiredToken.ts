export default function useIsExpiredToken () {
  //     useEffect(() => {
  //   if (user) getToken();
  // }, [])

  // useEffect(() => {
  //   if (user) {
  //     const interval = setInterval(() => {
  //       setIsExpired(checkExpirationDateJwt(user?.token));
  //       if (user && isExpired) getToken();
  //       setCheck(check + 1)
  //     }, 1000 * 60 * 2); // every 2 minutes
  //     return () => clearInterval(interval); 
  //   }
  // }, [check])
}